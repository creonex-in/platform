# Copy to .env.local. All vars are NEXT_PUBLIC (client-exposed).

# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# This web app's own public URL
NEXT_PUBLIC_WEB_URL=http://localhost:3001
