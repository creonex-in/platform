# Committed. Live production values — loaded by `next build`/`next start`.
# Vercel dashboard env vars override these if set. Keep non-secret only.

NEXT_PUBLIC_API_URL=https://api.creonex.in
NEXT_PUBLIC_WEB_URL=https://creonex.in
